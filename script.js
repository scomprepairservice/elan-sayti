const list=document.getElementById('list'),search=document.getElementById('search');function render(a){list.innerHTML=a.map(x=>`<article class="card"><img src="${x.image}" alt="${x.title}"><div><small>${x.category}</small><h3>${x.title}</h3><p>${x.location}</p><p>${x.description}</p><strong>${x.price}</strong><a class="btn" href="ad.html?id=${x.id}">Ətraflı</a></div></article>`).join('')}function filter(){let q=search.value.toLowerCase();render(ads.filter(x=>(x.title+x.category+x.location+x.description).toLowerCase().includes(q)))}search.oninput=filter;render(ads);
